import { makeStyles } from "@fluentui/react-components";

export const useTableGlobalRankStyles = makeStyles({
    card: { marginTop: '16px' },
    memberAN: { display: 'flex', alignItems: 'center', gap: '8px' },
});