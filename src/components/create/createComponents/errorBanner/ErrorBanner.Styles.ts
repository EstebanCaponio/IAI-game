import { makeStyles } from "@fluentui/react-components";

export const useErrorBannerStyles = makeStyles({
    bannerContainer:{display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: '12px', width: '100%'}
});