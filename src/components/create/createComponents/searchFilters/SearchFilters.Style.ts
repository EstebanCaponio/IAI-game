import { makeStyles } from "@fluentui/react-components";

export const useSearchFiltersStyles = makeStyles({
    container: { display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: '8px', marginBottom: '10px' }
});